// Anything exported from this file is importable by other in-browser modules.
// export function publicApiFunction() {}

export function RgetData(who) {

  console.log(`${who} is use me`);
  return who;
  
}

export function VgetData(who) {
  console.log(`${who} is use me`);
  return who
}